import * as  React from 'react';
import { View, Text } from 'react-native';

export const Menu = React.memo(() => {
    return <View style={{ flex: 1, marginTop: 114, padding: 8 }}>
        <Text>Menu</Text>
    </View>
})